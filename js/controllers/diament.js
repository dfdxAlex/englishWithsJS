function diament()
{
    let emodzi = '';
    let smile = '';
    let diament = 0;
    [emodzi, smile, diament] = diamentReturnStr();
    //diament = 10000;
    diamentView(diament, emodzi, smile);
}
